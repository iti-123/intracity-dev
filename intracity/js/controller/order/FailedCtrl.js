app.controller('FailedCtrl', ['$scope', '$http', 'config', 'consignment', 'apiServices', '$state', '$rootScope', function ($scope, $http, config, consignment, apiServices, $state, $rootScope) {

    var serverUrl = config.serverUrl;
    var authToken = config.appAuthToken;

}]);